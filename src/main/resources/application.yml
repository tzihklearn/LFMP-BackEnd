server:
  port: 38080
  servlet:
    encoding:
      charset: UTF-8
      force: true

spring:
  application:
    name: LFMP-BackEnd
  profiles:
    active: dev
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://118.195.251.126:3300/mmt?useUnicode=true&characterEncoding=UTF-8&serverTimezone=GMT%2B8
    username: root
    password: sipc115@mmt@mysql

logging:
  level:
    com.MinNiCup.lfmpbackend.mapper: debug

mybatis:
  mapper-locations: classpath:/mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true

pagehelper:
  helperDialect: mysql
  reasonable: true
  supportMethodsArguments: true
  params: count=countSql
